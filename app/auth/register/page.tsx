import React from 'react'

const RegisterPage = () => {
    return (
        <form
        // action={async () => {
        //   "use server"
        //   await signIn()
        // }}
      >
        <button type="submit">Sign in</button>
      </form>
    )
}

export default RegisterPage